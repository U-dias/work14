<div class="container ">
    <div class="has-bg-img">
  
        <h2 class="text-light font2" style="margin-left: 1rem;">
        </h2>
        <!--検索フォーム-->
        <div>
         <%= search_form_for @q, url: search_pages_path do |f| %>
          <!--name_contは、あいまい検索を指定-->
          <%= f.text_field :listing_name_cont %>
          <%= f.submit "フリーワード検索" %>
         <% end %>
         <%= search_form_for @q, url: search_pages_path do |f| %>
         <div>
          <%= f.label :address_cont, "住所を含むキーワード" %>
          <%= f.text_field :address_cont, value: params.dig(:q, :address_cont) %>
         </div>
         <div>
          <%= f.radio_button :address_cont, '東京都', checked: params.dig(:q, :address_eq) == '東京都' %> 東京
          <%= f.radio_button :address_cont, '大阪府', checked: params.dig(:q, :address_eq) == '大阪' %> 大阪
          <%= f.radio_button :address_cont, '京都府', checked: params.dig(:q, :address_eq) == '京都' %> 京都
          <%= f.radio_button :address_cont, '札幌', checked: params.dig(:q, :address_eq) == '札幌' %> 札幌
         </div>
          <%= f.submit "検索" %>
         <% end %>

        </div>
    </div>
</div>
<!-- ホーム -->
<% if params[:q].present? %>
<div class="container">
<% if @rooms.present? %>
    <div class="row mb-4">
        <div class="font1"><h3>宿泊可能な部屋</h3></div>
        <% @rooms.each do |room| %>
            <div class="col-md-4">
                <div class="card">
                    <div class="card-body">
                        <span><i class="fa fa-star fa-1x" style="color: gold;"></i></span>
                        <%= link_to room_path(room), data: { turbolinks: false} do %>
                            <%= image_tag room.photo.attached? ? room.photo : 'default_room.png', width: "100%", height: "100", style: "object-fit: contain; border-radius: 8px;" %>
                            <h5 class="card-title mt-2">
                                <span class="btn btn-light"><%= room.listing_name %></span>
                            </h5>
                        <% end %>
                        <div class="card-text" style="margin-left: 0.5rem;">
                            <p style="font-size: 0.8rem; margin-bottom: -0.3rem;">Address</p>
                            <p style="margin-bottom: 2rem;"><%= room.address %></p>
                            <%= link_to user_path(room.user), style: "text-decoration:none;" do %>
                                <figure class="figure">
                                    <%= image_tag avatar_url(room.user), style: "width: 40px;", class: "figure-img img-fluid rounded-pill" %>
                                </figure>
                                <span class="badge bg-light text-dark" style="font-size: 0.9rem;"><%= room.user.full_name %></span>
                            <% end %>                    
                        </div>
                        <h5 class="badge rounded-pill bg-danger text-light" style="font-size: 1rem;">1泊<%= number_to_currency(room.price) %></h5>
                    </div>
                </div>
            </div>
        <% end %>
        <%= link_to '戻る' %>
        </div>
    </div>
<% end %>
</div>
<script>
$('#start_date').datepicker({
  dateFormat: 'dd-mm-yy',
  minDate: 0,
  maxDate: '3m',
  onSelect: function(selected) {
    $('#end_date').datepicker("option", "minDate", selected);
    $('#end_date').attr("disabled", false);
  }
});
$('#end_date').datepicker({
  dateFormat: 'dd-mm-yy',
  minDate: 0,
  maxDate: '3m',
  onSelect: function(selected) {
    $('#start_date').datepicker("option", "maxDate", selected);
  }
});
</script>
<% end %>